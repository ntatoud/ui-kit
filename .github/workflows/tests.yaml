name: 🧪 Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  tests:
    timeout-minutes: 10
    name: ⚛️ Components Tests
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup

      - name: Run unit tests
        run: pnpm run test:ci